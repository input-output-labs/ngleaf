import { NgModule } from '@angular/core';
import {LeafLanguageService} from './language.service';

@NgModule({
    imports: [],
    providers: [LeafLanguageService]
})
export class LeafLanguageServiceModule {}
